version: "3"
services:
  api:
    image: azcoigreach/python-api
    ports:
      - 80:8000
    environment:
      - CLIENT_ID=${CLIENT_ID}
      - CLIENT_SECRET=${CLIENT_SECRET}
      - REDIRECT_URI=${REDIRECT_URI}
